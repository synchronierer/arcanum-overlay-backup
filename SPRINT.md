# ARC-CODEX-1A — Frontend workflow validation

## Goal

Establish a reliable repository-local Codex workflow without changing user
behavior.

## Tasks

1. Read `AGENTS.md`, `CODING_RULES.md`, and the documents under `docs/`.
2. Inspect the repository structure and correct documentation only where the
   repository proves it inaccurate.
3. Verify that `scripts/status`, `scripts/build-project`, and `scripts/check` work.
4. Document any missing automated tests or unclear architectural boundaries.
5. Do not change dashboard behavior, visual design, routes, or production
   runtime files in this sprint.
6. Do not commit or push.

## Completion report

Report changed files, commands executed, results, and concrete follow-up tasks.
